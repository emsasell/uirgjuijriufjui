"use client";import {useState} from 'react';export default function Verify(){const[code,setCode]=useState(''),[err,setErr]=useState('');async function go(e:any){e.preventDefault();const r=await fetch('/api/auth/verify',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})});const d=await r.json();if(!r.ok)return setErr(d.error||'Неверный код');location.href='/'}return <main className="auth"><div className="card" style={{width:'min(500px,100%)',padding:45}}><h1>Подтверждение почты</h1><p className="muted">Мы отправили 6-значный код на вашу почту.</p>{err&&<p className="error">{err}</p>}<form onSubmit={go}><div className="field"><label>Код подтверждения</label><input className="verifycode" inputMode="numeric" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))} placeholder="000000" required/></div><button className="btn" style={{width:'100%'}}>Подтвердить и войти</button></form></div></main>}