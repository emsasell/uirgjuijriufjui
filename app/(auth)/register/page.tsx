"use client";import Link from 'next/link';import {useState} from 'react';export default function Register(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[username,setUsername]=useState(''),[err,setErr]=useState('');async function go(e:any){e.preventDefault();setErr('');const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password,username})});const d=await r.json();if(!r.ok)return setErr(d.error||'Ошибка');location.href='/verify'}return <main className="auth"><div className="card authbox"><section className="intro"><img src="/emsell-logo.jpg"/><h1>EMSELL — твой рынок игровых товаров</h1><p className="muted">Создай аккаунт и публикуй товары для любимых игр.</p></section><form className="formside" onSubmit={go}><h1>Регистрация</h1>{err&&<p className="error">{err}</p>}<div className="field"><label>Юзернейм</label><input value={username} onChange={e=>setUsername(e.target.value.replace(/\s/g,''))} placeholder="emseller" minLength={3} required/></div><div className="field"><label>Почта</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Пароль</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required/></div><button className="btn">Зарегистрироваться</button><p className="muted">Есть аккаунт? <Link href="/login"><b>Войти</b></Link></p></form></div></main>}