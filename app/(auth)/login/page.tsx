"use client";import Link from 'next/link';import {useState} from 'react';export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(false);async function go(e:any){e.preventDefault();setLoading(true);setErr('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();setLoading(false);if(!r.ok)return setErr(d.error||'Ошибка');location.href='/verify'}return <main className="auth"><div className="card authbox"><section className="intro"><img src="/emsell-logo.jpg"/><h1>EMSELL — твой рынок игровых товаров</h1><h3 className="muted">Находи товары, аккаунты, серверы и услуги проще.</h3><p className="muted">Minecraft, Roblox и другие игры. Связь с продавцом напрямую через чат.</p></section><form className="formside" onSubmit={go}><h1>Вход в аккаунт</h1>{err&&<p className="error">{err}</p>}<div className="field"><label>Почта</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@email.com" required/></div><div className="field"><label>Пароль</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn" disabled={loading}>{loading?'Проверяем...':'Войти'}</button><p className="muted">Нет аккаунта? <Link href="/register"><b>Регистрация</b></Link></p></form></div></main>}